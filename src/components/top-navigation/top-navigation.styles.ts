import styled from 'styled-components';

export const StyledTopNavBackground = styled.header`
  display: flex;
  align-items: center;
  padding: 1rem;

  & > :first-child {
    margin-right: auto;
  }
`;
